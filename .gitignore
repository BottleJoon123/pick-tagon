node_modules/
supabase/.temp/
dist/

# Env files (may contain project URLs or email lists — use .env.example as template)
.env
.env.local
.env.*.local
# .env.example is intentionally tracked

# Claude CLI
.claude/
.claudeignore

# UFCStats scrape data (local only)
data/

# Python build artifacts
scripts/__pycache__/

# Design handoff screenshots (binary)
docs/design_handoff_picktagon/screenshots/

# Script output / dry-run reports (local only — large files, not committed)
reports/

# Local SEO/design asset packages (reference only, not deployed)
08_SEO_적용패키지/
