# QA Run — 2026-05-05 (Phase 4B — Profile RPC Frontend)

작성일: 2026-05-05
기준 커밋: `2d36f2e` (origin/main)

---

## 목적

Phase 4B `get_user_pick_stats` RPC의 DB 집계값이 profile.js 프론트 연결 후
실제 UI 표시값과 일치하는지 검증. RPC 실패 시 fallback 경로가 화면을 깨지 않는지 확인.

---

## 적용 커밋

| 커밋 | 내용 |
|------|------|
| `1c0e6d8` | Feat: Add user pick stats RPC (Phase 4B) |
| `2d36f2e` | Feat: Connect profile stats to get_user_pick_stats RPC (Phase 4B frontend) |

---

## 적용된 변경

- `supabase/migrations/20260504_get_user_pick_stats_rpc.sql` — DB RPC 등록
- `public/js/profile.js`, `dist/js/profile.js` — async dual-render 연결

---

## QA 케이스

### 케이스 1 — 실유저 스칼라 필드 (RPC vs 원본 쿼리 교차 검증)

대상: `ed396e42-3533-4c13-979f-d8c8b2affb4c` (28픽 유저)

| 필드 | RPC 반환 | 원본 직접 집계 | 일치 |
|------|----------|----------------|------|
| settled_picks | 21 | 21 | ✅ |
| win_count | 12 | 12 | ✅ |
| lose_count | 7 | 7 | ✅ |
| cancel_count | 2 | 2 | ✅ |
| pending_count | 7 | 7 | ✅ |
| accuracy | 63 | 63 (12/19×100) | ✅ |
| net_points | 2090 | 2090 | ✅ |
| upset_wins | 0 | 0 | ✅ |
| upset_picks | 0 | 0 | ✅ |

---

### 케이스 2 — by_weight_class 교차 검증

| weight_class | RPC win/total/acc | 원본 집계 | 일치 |
|---|---|---|---|
| mw | 2/3/100% | 2/3/100% | ✅ |
| bw | 3/3/100% | 3/3/100% | ✅ |
| fw | 2/3/67% | 2/3/67% | ✅ |
| ww | 1/2/50% | 1/2/50% | ✅ |
| lhw | 1/2/100% | 1/2/100% | ✅ |
| lw | 1/1/100% | 1/1/100% | ✅ |
| hw | 0/1/0% | 0/1/0% | ✅ |
| wbw | 0/1/0% | 0/1/0% | ✅ |
| flw | 0/1/0% | 0/1/0% | ✅ |

참고: `lhw` total=2, accuracy=100% — win=1, lose=0, 나머지 1픽은 pending.
`by_weight_class.total`은 참여 픽 전체 수 (pending 포함) 설계대로 동작.

---

### 케이스 3 — by_method 교차 검증

| method | RPC win/total | 원본 집계 | 일치 |
|---|---|---|---|
| UD | 7/10 | 7/10 | ✅ |
| KO/TKO | 3/6 | 3/6 | ✅ |
| SUB | 1/1 | 1/1 | ✅ |

참고: QA 중 `actual_method='NC'` AND `status='win'` 픽 1건이 발견됨 — KDI-02 repair 후 stale actual result 필드.
해당 매치업의 공식 결과는 `completed / TalitaAlencar / UD`이고, pick 71의 `status='win'`과 payout은 정상.
`20260505_kdi03_repair_pick71_actual_result.sql` 적용 후 pick 71은 `actual_winner='TalitaAlencar'`,
`actual_method='UD'`로 수정되었고, `actual_method='NC'` AND `status='win'` 조회는 0건.

---

### 케이스 4 — profile.js 렌더 로직 매핑 검토

**renderProfileReport()**

| profile.js 변수 | 소스 | 표시 결과 |
|---|---|---|
| total | `win_count + lose_count` = 19 | "19회" |
| success | `win_count` = 12 | "12승 7패" |
| acc | `accuracy` = 63 | "63%" |
| totalEarned | `net_points` = 2090 | "2,090P" |
| upsetWins | `upset_wins` = 0 | "0회" |
| methodBonusCount | state-only (RPC 해당 없음) | state 유지 |

**renderDivisionStats()**
- 9개 체급 표시, ORDER BY total DESC ✅
- 레이블: `WEIGHT_CLASS_LABEL['mw']` → `'Middleweight'` → `.toUpperCase()` → `'MIDDLEWEIGHT'` ✅
- 표시 형식: `win_count/total` (예: "2/3") — total은 참여 픽 전체 ✅
- 정확도 색상 분기: ≥70% ufcRed, ≥50% white, <50% gray ✅

**renderMethodStats()**
- KO/TKO: 3/6 → 50% ✅
- SUB: 1/1 → 100% ✅
- UD: 7/10 → 70% ✅

---

### 케이스 5 — fallback 경로 코드 리뷰

| 시나리오 | 동작 | 결과 |
|---|---|---|
| `currentUser` 없음 (비로그인) | `currentUser?.id` null → RPC 미호출, `_rpcStats = null` 유지 | state-only 렌더 ✅ |
| RPC 에러 반환 | `if (!error && data)` 조건 false → `_rpcStats` 갱신 안 됨 | state-only 렌더 ✅ |
| RPC 정상 → `_rpcStats` 설정 | `_rpcStats != null` → RPC 경로 렌더 덮어쓰기 | RPC 기반 렌더 ✅ |
| `renderFormChart()` | `_rpcStats` 참조 없음, state-only | 변경 없음 ✅ |
| `renderBonusSummary()` | `_rpcStats` 참조 없음, state-only | 변경 없음 ✅ |

---

## 데이터 이상 기록

| ID | 설명 | 영향 | 처리 |
|----|------|------|------|
| KDI-03 | `picks` 1건: `actual_method='NC'`, `status='win'` — pick 71의 actual result 필드가 KDI-02 repair 이전 NC 값으로 남음 | repair 전 UI 미표시 (NC methods 배열 외) | `20260505_kdi03_repair_pick71_actual_result.sql` 적용 완료 |

### KDI-03 진단

- 대상: pick `71`, matchup `3006a883-feb5-423f-ae84-d44aa45771ee`
- matchup 공식 결과: `completed`, winner=`TalitaAlencar`, side=`red`, method=`UD`
- pick 현재 상태: `status='win'`, `payout=190`, `settled_payout=190` 정상
- 이상 필드: `actual_winner='NC'`, `actual_method='NC'`
- 원인: KDI-02 repair에서 pick 71을 `cancelled → win`으로 전환하면서 actual result 필드를 함께 갱신하지 않음
- repair 범위: `actual_winner`, `actual_method`만 matchup 공식 결과와 동기화
- 포인트/status/payout 조정: 없음
- 적용 후 확인: `actual_method='NC'` AND `status='win'` 0건, pick 71 `actual_winner='TalitaAlencar'`, `actual_method='UD'`

---

## 결론

| 항목 | 결과 |
|------|------|
| 스칼라 필드 RPC vs 원본 일치 | PASS |
| by_weight_class 전수 일치 | PASS |
| by_method 전수 일치 | PASS |
| profile.js 렌더 로직 매핑 | PASS |
| fallback 경로 (비로그인/RPC 실패) | PASS |
| 데이터 이상 발견 | KDI-03 repair 적용 완료 (포인트/status/payout 영향 없음) |

**전체 PASS. Phase 4B frontend 연결 완료.**
