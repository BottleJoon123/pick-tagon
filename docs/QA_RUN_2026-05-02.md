# QA Run Report — 2026-05-02

**실행일:** 2026-05-02  
**실행자:** Claude Code (automated DB-level QA)  
**대상 프로젝트:** rnnrimzrypayvnmznpin  
**범위:** Event Lifecycle Phase 1 + 핵심 사용자 플로우 (T-01 ~ T-11)  
**방법:** Supabase MCP `execute_sql` — JWT 시뮬레이션 + 임시 테이블 결과 수집

---

## 요약

| 구분 | 수 |
|---|---|
| 전체 테스트 | 11 |
| PASS | 9 |
| FAIL | 1 |
| UNEXPECTED (버그 확인됨) | 1 |
| 미실행 (브라우저 필요) | 0 |

**CRITICAL 버그 1건 발견 → 즉시 수정 필요 (ISSUE-03 참조)**

---

## 테스트 계정

| 역할 | nickname | user_id | points |
|---|---|---|---|
| Admin | KINGBOTTLE | `ed396e42-3533-4c13-979f-d8c8b2affb4c` | 2925 |
| 일반 유저 | 보틀준 | `275cb9d4-d682-4ef6-84da-426df52fdd63` | 935 |

---

## 테스트 데이터 (QA 전용, 정리 필요)

| 종류 | 값 |
|---|---|
| Event title | `QA_TEST_EVENT_20260502` |
| Event ID | `96aa21a2-a7e0-413c-bda0-bacb3d3a0428` |
| Matchup | `QA_RedFighter vs QA_BlueFighter` |
| Matchup ID | `9bd4ca69-3fa5-478f-a04d-97af1b43e7cc` |
| 현재 상태 | `locked` (T-10 실행 후) |

> QA 종료 후 이 이벤트/매치업을 삭제하거나 archived 처리 권장.

---

## 테스트 결과 상세

### T-01 — 비관리자 pick 마감 차단
- **목표:** 일반 유저가 `admin_lock_event_picks` 호출 시 거부
- **결과:** ✅ PASS
- **에러:** `admin_required`
- **비고:** SECURITY DEFINER + `private.is_admin()` 정상 작동

### T-02 — 비관리자 이벤트 정산 차단
- **목표:** 일반 유저가 `admin_settle_event` 호출 시 거부
- **결과:** ✅ PASS
- **에러:** `admin_required`

### T-03 — 비관리자 아카이브 차단
- **목표:** 일반 유저가 `admin_archive_event` 호출 시 거부
- **결과:** ✅ PASS
- **에러:** `admin_required`

### T-04 — 비관리자 이벤트 upsert 차단
- **목표:** 일반 유저가 `admin_upsert_event` 호출 시 거부
- **결과:** ✅ PASS
- **에러:** `admin_required`

### T-05 — 정상 예측 등록
- **목표:** 일반 유저가 `place_pick` 호출 → picks INSERT 성공, 포인트 차감
- **결과:** ❌ FAIL — **CRITICAL BUG**
- **에러:** `invalid input syntax for type uuid: "77"`
- **원인 분석:** `place_pick` 함수 내 `v_pick_id UUID` 선언이 있으나 `picks.id`는 `bigint` 타입. `RETURNING id INTO v_pick_id` 실행 시 bigint autoincrement 값을 UUID로 캐스팅 시도 → 타입 불일치 오류.
- **영향:** 모든 픽 등록이 불가능한 **서비스 중단 수준** 버그. → ISSUE-03 참조

### T-06 — 중복 예측 차단
- **목표:** 같은 fight에 두 번 픽 등록 시 `duplicate_pick` 반환
- **결과:** ⚠️ UNEXPECTED
- **에러:** `invalid input syntax for type uuid: "78"`
- **원인:** T-05와 동일 — place_pick의 UUID/bigint 타입 불일치가 첫 번째 INSERT에서 이미 실패하므로 중복 체크 로직 도달 불가. 버그가 중복 차단 테스트 자체를 불가능하게 만듦.
- **비고:** 중복 차단 SQL 로직 자체는 코드상 올바름 (INSERT 전에 EXISTS 체크 존재). 버그 수정 후 재테스트 필요.

### T-07 — 포인트 부족 차단
- **목표:** 포인트 < bet_cost 시 `insufficient_points` 반환
- **결과:** ✅ PASS
- **에러:** `insufficient_points`
- **비고:** 포인트 체크가 INSERT보다 먼저 실행되므로 UUID 버그와 무관하게 정상 작동

### T-08 — 픽 마감 후 예측 차단
- **목표:** `picks_locked_at IS NOT NULL`인 이벤트의 matchup에 픽 시도 → `pick_locked`
- **결과:** ✅ PASS
- **에러:** `pick_locked`
- **비고:** Phase 1 신규 보호 로직 정상 작동 확인. UUID 버그보다 앞에서 체크함.

### T-09 — 픽 재오픈 (admin_reopen_event_picks)
- **목표:** locked 이벤트를 upcoming으로 되돌리기
- **결과:** ✅ PASS
- **반환:** `{"ok": true, "idempotent": false}`
- **비고:** status=upcoming, picks_locked_at=NULL 확인됨

### T-10 — 미결 matchup 존재 시 settle 차단
- **목표:** locked 이벤트에 result_status='scheduled' matchup이 있을 때 `admin_settle_event` → 거부
- **결과:** ✅ PASS
- **에러:** `event_has_unresolved_matchups`
- **비고:** Phase 1 Risk Fix 1 정상 작동. QA 이벤트는 이 테스트 후 locked 상태로 유지됨.

### T-11 — 미정산 이벤트 archive 차단
- **목표:** settled가 아닌 이벤트에 `admin_archive_event` 호출 시 거부
- **결과:** ✅ PASS
- **에러:** `event_not_settled: cannot archive an event with status locked`
- **비고:** Phase 1 Risk Fix 2 정상 작동 (completed 상태도 직접 archive 불가)

---

## get_leaderboard 보안 확인

- **목표:** `is_admin` 컬럼이 리더보드 응답에 포함되지 않아야 함
- **결과:** ✅ PASS
- **반환 컬럼:** `rank, points, nickname, total_picks, success_picks` — is_admin 없음

---

## 발견된 이슈

### ISSUE-03 — CRITICAL: place_pick UUID/bigint 타입 불일치

| 항목 | 내용 |
|---|---|
| 심각도 | CRITICAL |
| 영향 | 모든 픽 등록 불가 (서비스 중단 수준) |
| 발생 위치 | `place_pick` 함수 내 `RETURNING id INTO v_pick_id` |
| 원인 | `v_pick_id UUID`로 선언했으나 `picks.id`는 `bigint` (autoincrement) |
| 존재 파일 | `supabase/migrations/20260428_place_pick_rpc.sql` (원본) 및 `20260502_event_lifecycle_phase1.sql` (Phase 1 복제본) 모두 동일 버그 보유 |
| 수정 방법 | `DECLARE` 섹션에서 `v_pick_id UUID` → `v_pick_id BIGINT` 변경, `RETURNING id INTO v_pick_id` 유지, 반환값 `jsonb_build_object('ok', true, 'pick_id', v_pick_id, ...)` 그대로 사용 |
| 수정 우선순위 | 즉시 |

**수정 전 (현재):**
```sql
DECLARE
    ...
    v_pick_id     UUID;
    ...
```

**수정 후:**
```sql
DECLARE
    ...
    v_pick_id     BIGINT;
    ...
```

---

### ISSUE-01 — HIGH: 고아 pending picks (정산 미완료 이벤트)

| 항목 | 내용 |
|---|---|
| 심각도 | HIGH |
| 영향 | 유저 포인트 환급 미처리 |
| 내용 | UFC 273, UFC 274 이벤트에 pending 상태 픽 4건 잔존 |
| 원인 | 해당 이벤트들이 completed 상태이나 `admin_settle_event` 미실행 |
| 조치 | Admin 패널에서 두 이벤트에 `admin_settle_event` 실행 → pending picks cancelled 처리 + 포인트 환급 |

---

### ISSUE-02 — LOW: users 테이블 중복/중첩 RLS 정책

| 항목 | 내용 |
|---|---|
| 심각도 | LOW |
| 영향 | 성능 미세 저하 가능, 정책 관리 복잡도 증가 |
| 내용 | 구형 `유저 본인만 수정 ALL` 정책과 신형 세분화 정책이 공존 |
| 조치 | 구형 ALL 정책 제거 후 신형 정책만 유지 (기능 영향 없음, 정리 목적) |

---

## 미실행 테스트 (재실행 필요)

| 테스트 | 이유 | 우선순위 |
|---|---|---|
| T-06 중복 픽 차단 | ISSUE-03 수정 후 재테스트 필요 | ISSUE-03 수정 후 즉시 |
| T-05 정상 픽 등록 E2E | ISSUE-03 수정 후 재테스트 필요 | ISSUE-03 수정 후 즉시 |
| users RLS 교차 유저 보호 | execute_sql로 두 세션 동시 시뮬레이션 제한 | 중간 |
| 모바일/반응형 | 브라우저 직접 확인 필요 | 중간 |

---

## 권장 후속 조치 (우선순위 순)

1. **즉시** — ISSUE-03 fix: `place_pick`에서 `v_pick_id UUID` → `v_pick_id BIGINT` 변경, migration 재적용
2. **즉시** — ISSUE-03 수정 후 T-05/T-06 재테스트
3. **단기** — Admin 패널에서 UFC 273, UFC 274 `admin_settle_event` 실행 (ISSUE-01)
4. **단기** — QA 테스트 데이터(`QA_TEST_EVENT_20260502`) 아카이브 또는 삭제
5. **장기** — ISSUE-02: users RLS 구형 정책 정리

---

*Report generated by Claude Code automated QA session.*
